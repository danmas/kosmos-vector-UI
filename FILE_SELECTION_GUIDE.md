# 🎯 Knowledge Processing Pipeline - File Selection Guide

## Новая функциональность: Умный выбор файлов (v2.1.1)

Теперь пользователи могут точно контролировать, какие файлы обрабатываются в Knowledge Processing Pipeline с помощью двух режимов работы!

## 🎯 Два режима выбора файлов

### Режим 1: 🎯 По маске (Glob Patterns)
- Автоматический выбор файлов по glob-паттернам
- Мгновенное применение без перезагрузки дерева
- Примеры масок: `**/*.sql`, `**/*api_*.sql`, `**/*.{py,js,ts}`

### Режим 2: ✋ Ручной выбор (Manual Selection)
- Точный выбор файлов через чекбоксы
- Сохранение выбора на сервер
- Восстановление после перезагрузки страницы

## 🔧 Как использовать:

### 1. **Перейти на страницу "Files"**
- Откройте приложение в браузере
- Нажмите на вкладку "Files" в боковой панели

### 2. **Сканировать проект**
- Введите путь к проекту (например: `C:\path\to\your\project`)
- Нажмите **"Scan Folder"** (дерево не перерисовывается при вводе!)
- Дождитесь загрузки дерева файлов

### 3. **Выбрать файлы для обработки**

#### Вариант A: 🎯 Выбор по маске
1. Введите glob-паттерн в поле **"Include Mask"** (например: `**/*api_*.sql`)
2. Файлы автоматически выберутся через 300ms
3. Индикатор **"🎯 По маске"** покажет активный режим

#### Вариант B: ✋ Ручной выбор
1. Отметьте чекбоксы рядом с нужными файлами
2. Индикатор переключится на **"✋ Ручной выбор"**
3. Выбор автоматически сохранится на сервер
4. После перезагрузки страницы выбор восстановится!

### 4. **Запустить Processing**
- Нажмите кнопку **🚀 "Start Processing"**
- Система автоматически переключится на Pipeline Monitor
- Наблюдайте за прогрессом обработки в реальном времени

## 🔍 Что происходит за кулисами:

### **Локальная фильтрация (v2.1.1)**
- Маски применяются **мгновенно на клиенте** без запросов к серверу
- Используется библиотека `minimatch` для glob-matching в браузере
- Дерево файлов не перезагружается при изменении маски

### **Сохранение конфигурации**
- Конфигурация сохраняется на сервер через `/api/kb-config`
- Сообщение **"Сохранено!"** отображается в заголовке справа
- `fileSelection` сохраняется автоматически при ручном выборе

### **API Changes:**
```javascript
// Новый формат запроса к /api/pipeline/start
{
  "projectPath": "C:\\path\\to\\project",
  "filePatterns": ["**/*.{py,ts,js,go,java}"],
  "selectedFiles": [                    // ← Приоритет над filePatterns!
    "./src/main.py",
    "./lib/utils.ts"
  ],
  "excludedFiles": [],
  "forceReparse": true
}
```

### **Processing Logic:**
1. **Если `fileSelection` не пустой** → используются только файлы из списка (Режим 1: Точный выбор)
2. **Если `fileSelection` пустой** → используются `filePatterns` из `includeMask` (Режим 2: Glob-маски)
3. **Исключенные файлы** → всегда игнорируются

### **UI Improvements:**
- ✅ Счетчик выбранных файлов: `"Selected: 15 of 42 files"`
- 🎯 Индикатор режима: **"🎯 По маске"** или **"✋ Ручной выбор"**
- 💾 Сообщение **"Сохранено!"** в заголовке (не прыгает дерево)
- ⚡ Мгновенное применение маски без перезагрузки
- 🔄 Восстановление ручного выбора после перезагрузки страницы
- 📁 **Игнорируемые папки схлопнуты**: Папки, попадающие в `ignorePatterns`, отображаются свернутыми (collapsed) по умолчанию
- 🚫 **Умные игноры по умолчанию**: `.cursor`, `.git`, `.vscode`, `node_modules`, `venv` и другие служебные папки автоматически игнорируются

## 🚀 Преимущества новой системы:

- **🎯 Точность**: Обрабатывайте только нужные файлы
- **⚡ Скорость**: Меньше файлов = быстрее обработка
- **💰 Экономия**: Меньше LLM запросов = меньше затрат
- **🔧 Гибкость**: Легко исключить тестовые/временные файлы
- **👀 Прозрачность**: Видно точно что обрабатывается
- **💾 Сохранение**: Ручной выбор сохраняется и восстанавливается
- **⚡ Мгновенность**: Маски применяются локально без задержек
- **📁 Чистое дерево**: Служебные папки (`.git`, `.vscode`, `.cursor`) автоматически игнорируются и схлопнуты

## 🔄 Обратная совместимость:

- Старые методы работают как прежде
- Если `fileSelection` пуст → используются `filePatterns` из `includeMask`
- API автоматически определяет режим обработки
- При изменении маски пользователем — автоматически переключается на режим маски

## 📝 Примеры использования:

### Пример 1: Выбор всех SQL файлов с "api" в имени
```
Include Mask: **/*api*.sql
Режим: 🎯 По маске
```

### Пример 2: Ручной выбор конкретных файлов
1. Откройте дерево файлов
2. Отметьте нужные файлы чекбоксами
3. Режим автоматически переключится на **✋ Ручной выбор**
4. Выбор сохранится и восстановится после перезагрузки

### Пример 3: Комбинированный подход
1. Начните с маски `**/*.sql` для быстрого выбора всех SQL файлов
2. Затем вручную снимите ненужные файлы
3. Система переключится на ручной выбор и сохранит результат

## 🚫 Игнорируемые папки по умолчанию

Следующие папки автоматически игнорируются и отображаются схлопнутыми:

- **`.cursor`** — настройки Cursor IDE
- **`.git`** — репозиторий Git
- **`.vscode`** — настройки VS Code
- **`node_modules`** — зависимости Node.js
- **`venv`**, **`env`** — виртуальные окружения Python
- **`__pycache__`** — кэш Python
- **`dist`**, **`build`** — собранные файлы
- **`tests`** — тестовые файлы (если указаны в ignorePatterns)

Вы можете добавить свои паттерны в поле **"Ignore Patterns"** для исключения других папок.

---

**Готово! Теперь у вас есть полный контроль над тем, какие файлы попадают в Knowledge Base! 🎉**
