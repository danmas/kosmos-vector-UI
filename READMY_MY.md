[1 tool called]

Команда `npm run dev` запускает **Vite development server** для фронтенда.

Согласно `package.json`:
```json
"scripts": {
  "dev": "vite",
  ...
}
```

**Что это означает:**
- 🚀 Запускается **Vite** - современный и быстрый dev server для фронтенда
- 📱 Обслуживает React приложение в режиме разработки
- 🔄 Поддерживает **Hot Module Replacement** (HMR) - мгновенные обновления при изменении кода
- 🌐 Обычно доступен по адресу `http://localhost:5173` (стандартный порт Vite)

**Для полного запуска системы нужно:**
1. **Backend**: `node backend/server.js` (порт по умолчанию 3200, настраивается через `PORT_DATA_SERVER`)
2. **Frontend**: `npm run dev` (порт 5173)

**Другие доступные команды:**
- `npm run build` - сборка продакшн версии фронтенда
- `npm run build:lib` - сборка npm пакета UI компонентов через Rollup
- `npm run preview` - предварительный просмотр собранной версии

Таким образом, `npm run dev` - это команда для запуска фронтенд части приложения в режиме разработки.

---

## Архитектура 

Конфигурация Vite настроена корректно:

```typescript
server: {
  proxy: {
    '/api': {
      target: `http://localhost:${process.env.PORT_DATA_SERVER || 3200}`,
      changeOrigin: true,
      secure: false,
    },
  },
}
```

**Как это работает:**

1. **Frontend (Vite)** запускается на `http://localhost:5173`
2. **Backend (Node.js)** запускается на порту, указанном в `PORT_DATA_SERVER` (по умолчанию 3200)
3. **Proxy перенаправляет** все запросы с `/api/*` на backend сервер

**Пример работы:**
- UI делает запрос: `fetch('/api/pipeline')`
- Vite перехватывает и перенаправляет на: `http://localhost:${PORT_DATA_SERVER || 3200}/api/pipeline`
- Backend обрабатывает запрос и возвращает ответ
- UI получает ответ как будто запрос был локальным

**Параметры proxy:**
- `target` - адрес backend сервера, читается из `PORT_DATA_SERVER` (по умолчанию 3200) ✅
- `changeOrigin: true` - изменяет заголовок Origin для корректной работы CORS ✅  
- `secure: false` - отключает проверку SSL (для HTTP) ✅

**Это стандартная и правильная настройка для разработки**, которая позволяет избежать проблем с CORS и упрощает работу с API во время разработки.